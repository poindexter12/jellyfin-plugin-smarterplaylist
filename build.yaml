---
name: "Smarter Playlist"
guid: "3311dfd2-fe3b-4367-a3f0-0dcea5ba07cd"
version: "2.10.1"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Create dynamic playlists from rules"
description: >
  SmarterPlaylist lets you create dynamic playlists based on rules and conditions (genre, people, ratings, dates, play state, and more). A scheduled task keeps the generated playlists refreshed automatically.

category: "General"
owner: "poindexter12"
# Shown on the plugin's card in Jellyfin's catalogue. Served from the repository rather than a
# release asset so it is not tied to a version: the catalogue reads one image for the plugin, not
# one per release. 1920x1080 to match every plugin in Jellyfin's own manifest.
imageUrl: "https://raw.githubusercontent.com/poindexter12/jellyfin-plugin-smarterplaylist/main/assets/logo.png"
artifacts:
  - "Jellyfin.Plugin.SmarterPlaylist.dll"
  - "logo.png"
changelog: |-

  ### Code or Repo Maintenance ###
  - Ship the logo in the package instead of relying on a download (#28) @poindexter12
